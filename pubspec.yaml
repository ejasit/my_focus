name: focus_app
description: Focus Sessions App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  screen_retriever: ^0.2.0
  window_manager: ^0.5.1
  shared_preferences: ^2.2.0
  file_picker: ^11.0.2

flutter:
  uses-material-design: true